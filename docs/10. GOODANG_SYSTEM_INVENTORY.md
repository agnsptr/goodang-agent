# GOODANG SYSTEM INVENTORY

**Project:** Goodang  
**Purpose:** Inventory of systems, services, data sources, interfaces, ownership, and implementation readiness for Autonomous CS Goodang.  
**Primary Channel:** Telegram  
**Agent Framework:** Google Agent Development Kit (ADK)  
**Workflow Engine:** Temporal  
**Primary Business Process:** CS Goodang — Guru Ingin Pesan  
**Primary Transaction Log:** `POS_TRANSACTION_LOG`  
**Document Type:** System / Integration Inventory  
**Version:** 1.0  
**Status:** Baseline — requires environment verification before production implementation

---

# 1. Purpose

Dokumen ini menjadi **jembatan antara spesifikasi arsitektur Goodang dan environment Goodang yang benar-benar tersedia**.

Dokumen ini menjawab:

```text
SISTEM APA YANG ADA?
        ↓
DATA DIMANA?
        ↓
API APA YANG TERSEDIA?
        ↓
SIAPA YANG MEMILIKI DATA?
        ↓
BAGAIMANA AGENT MENGAKSESNYA?
        ↓
BAGAIMANA TEMPORAL MENJALANKANNYA?
```

Dokumen ini digunakan oleh:

```text
ADK Developer
Backend Developer
Temporal Developer
DevOps
QA
Hermes Agent
CS / Product Owner
```

---

# 2. Source of Truth Rule

Inventory harus selalu membedakan:

```text
CONFIRMED
```

dengan:

```text
ASSUMED
```

dan:

```text
TBD
```

Jangan menganggap suatu API/database/service tersedia hanya karena disebut dalam specification.

Sebelum implementasi production:

```text
TBD
 ↓
VERIFY
 ↓
CONFIRMED
```

---

# 3. Current Confirmed Business Architecture

Dari SOP Goodang yang sudah tersedia, arsitektur bisnis minimal terdiri dari empat lapisan:

```text
1. Conversation Layer
2. Order Layer
3. Master Data Layer
4. Transaction Layer
```

SOP secara eksplisit menetapkan bahwa:

```text
Chat ≠ Order ≠ Transaction
```

dan `POS_TRANSACTION_LOG` diperlakukan sebagai ledger transaksi final, bukan penyimpanan percakapan atau draft. fileciteturn2file4L543-L569

---

# 4. System Landscape

## 4.1 Logical Landscape

```text
                         CUSTOMER / GURU
                               │
                               ▼
                           TELEGRAM
                               │
                               ▼
                        TELEGRAM WEBHOOK
                               │
                               ▼
                           ADK AGENT
                               │
                       Structured Command
                               │
                               ▼
                     APPLICATION / TOOL LAYER
                               │
                               ▼
                           TEMPORAL
                               │
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
           MEMBER          PRODUCT           STOCK
           SOURCE           SOURCE            SOURCE
              │                │                │
              └────────────────┼────────────────┘
                               ▼
                             PRICE
                               │
                               ▼
                         PAYMENT/PLAFON
                               │
                               ▼
                        ORDER / TRANSACTION
                               │
                               ▼
                     POS_TRANSACTION_LOG
```

SOP mendefinisikan alur bisnis:

```text
Terima
→ Identifikasi
→ Pahami
→ Validasi
→ Susun Order
→ Konfirmasi
→ Buat Transaksi
→ Proses
→ Tutup Order
```

fileciteturn2file1L195-L225

---

# 5. System Inventory Summary

| ID | System / Component | Role | Status |
|---|---|---|---|
| SYS-001 | Telegram | Communication channel | CONFIRMED |
| SYS-002 | Telegram Webhook | Inbound message transport | TARGET / VERIFY |
| SYS-003 | ADK Agent | Conversation intelligence | TARGET |
| SYS-004 | Application / Tool Layer | API adapter + validation | TARGET |
| SYS-005 | Temporal | Durable workflow/state | TARGET |
| SYS-006 | Member Master / Service | Customer source of truth | REQUIRED / VERIFY |
| SYS-007 | Product Master | Product source of truth | REQUIRED / VERIFY |
| SYS-008 | Price Master / Service | Price source of truth | REQUIRED / VERIFY |
| SYS-009 | Stock Service | Stock source of truth | REQUIRED / VERIFY |
| SYS-010 | Payment / Plafon | Payment source of truth | REQUIRED / VERIFY |
| SYS-011 | Order Draft Store / Service | Draft order state | REQUIRED / VERIFY |
| SYS-012 | Transaction Service | Final transaction mutation | REQUIRED / VERIFY |
| SYS-013 | `POS_TRANSACTION_LOG` | Final transaction ledger | CONFIRMED |
| SYS-014 | Audit Log | Workflow/audit trail | REQUIRED / VERIFY |
| SYS-015 | Human CS / Handover | Exception handling | REQUIRED |
| SYS-016 | Monitoring / Observability | Operations | REQUIRED / VERIFY |

---

# 6. SYS-001 — Telegram

## Purpose

Channel komunikasi customer → Goodang CS.

## Confirmed

Goodang ecosystem documentation menunjukkan Telegram sebagai salah satu channel untuk interaksi/notifikasi. fileciteturn1file2L33-L33

Untuk proses CS pemesanan yang didefinisikan di dokumen ini, Telegram ditetapkan sebagai primary channel.

## Responsibilities

```text
receive customer message
send customer response
provide chat/user identity
provide update_id
```

## Should NOT Own

```text
order state
transaction
stock
price
payment
business rules
```

## Required Verification

```text
BOT TOKEN
BOT ID
WEBHOOK/POLLING MODE
CURRENT BOT OWNER
CURRENT BOT SERVICE
```

Status:

```text
CONFIRMED AS CHANNEL
IMPLEMENTATION DETAILS = VERIFY
```

---

# 7. SYS-002 — Telegram Webhook

## Purpose

Menerima update Telegram melalui HTTPS.

## Responsibilities

```text
validate webhook secret
validate Telegram update
extract chat_id
extract user_id
extract update_id
deduplicate
route message
```

## Required Interface

```text
POST /webhook/telegram
```

Exact production URL:

```text
TBD
```

## Required Configuration

```text
TELEGRAM_BOT_TOKEN
TELEGRAM_WEBHOOK_SECRET
TELEGRAM_WEBHOOK_URL
```

## Security

Webhook harus:

```text
HTTPS
secret token validation
rate limiting / abuse protection where required
no secret logging
```

---

# 8. SYS-003 — ADK Agent

## Role

ADK menjadi **conversation intelligence layer**.

ADK bertanggung jawab terhadap:

```text
natural language understanding
intent detection
entity extraction
conversation context
tool selection
customer-facing response
```

Ini konsisten dengan pembagian yang sudah ditetapkan pada specification Goodang. fileciteturn2file9L1354-L1377

## ADK Must NOT Own

```text
final transaction state
final transaction mutation
stock deduction
payment execution
POS_TRANSACTION_LOG
```

## Required Components

```text
root_agent
instruction
session
state
tools
schemas
callbacks
evaluation
```

Status:

```text
TARGET
```

---

# 9. SYS-004 — Application / Tool Layer

## Purpose

Menyediakan boundary antara ADK dan backend Goodang.

Architecture:

```text
ADK
 ↓
Tool
 ↓
Integration Client
 ↓
Goodang API
```

## Responsibilities

```text
input validation
authorization
normalization
API request
API response normalization
error mapping
correlation ID
audit
```

## Must NOT

```text
invent data
bypass business rule
write POS_TRANSACTION_LOG directly
```

Status:

```text
TARGET / REQUIRED
```

---

# 10. SYS-005 — Temporal

## Role

Temporal adalah authority untuk durable workflow/state.

Tugas:

```text
workflow state
durable waiting
retry
timeout
recovery
transaction orchestration
stock orchestration
payment orchestration
idempotency
```

## Required Configuration

```text
TEMPORAL_ADDRESS
TEMPORAL_NAMESPACE
TEMPORAL_TASK_QUEUE
```

Recommended:

```text
namespace:
goodang

task queue:
goodang-order
```

Status:

```text
TARGET
```

---

# 11. SYS-006 — Member Master / Member Service

## Purpose

Source of truth customer.

## Required Operations

```text
identify_customer
get_customer
```

## Minimum Required Data

```text
member_id
member_name
```

SOP menetapkan `member_id` dan `member_name` berasal dari Master Member. fileciteturn2file0L106-L126

## Required Verification

Hermes/developer harus menemukan:

```text
actual table/API
primary key
Telegram mapping
phone mapping
school/outlet relation
customer status
authentication
```

## Unknown

```text
API endpoint = TBD
database table = TBD
schema = TBD
```

Do not guess.

---

# 12. SYS-007 — Product Master

## Purpose

Source of truth product.

## Required Operations

```text
search_product
get_product
```

## Minimum Required Data

```text
sku_code
product_name
uom
```

SOP menetapkan:

```text
sku_code → Master Product
product_name → Master Product
uom → Master Product
```

fileciteturn2file0L106-L126

## Required Verification

```text
actual product table/API
SKU primary key
product active/inactive
searchable name
UOM model
barcode if available
category if available
```

Unknown:

```text
API endpoint = TBD
database/table = TBD
```

---

# 13. SYS-008 — Price Master / Pricing Service

## Purpose

Source of truth current selling price.

## Required Operations

```text
get_price
```

## Minimum Required Data

```text
sku_code
selling_price
service_fee
currency
```

SOP menetapkan `selling_price` berasal dari Price Master dan `service_fee` berasal dari Business Rule. fileciteturn2file0L108-L126

## Required Verification

```text
current price source
customer-specific pricing?
member tier?
effective date?
price history?
service fee formula?
```

The formula for `service_fee` is not established by the current SOP; therefore:

```text
SERVICE_FEE_FORMULA = TBD
```

Do not infer.

---

# 14. SYS-009 — Stock Service

## Purpose

Source of truth stock availability and final stock mutation.

## Required Operations

Read:

```text
check_stock
```

Mutation:

```text
deduct_stock
```

## Rules

`check_stock`:

```text
READ ONLY
```

Stock deduction:

```text
AFTER TRANSACTION SUCCESS
```

SOP explicitly states stock deduction occurs after transaction is successfully created. fileciteturn2file0L130-L148

## Required Verification

```text
stock table/service
available quantity field
outlet relation
concurrency behavior
reservation support?
stock deduction API
idempotency support
```

Reservation model:

```text
TBD
```

---

# 15. SYS-010 — Payment / Plafon

## Purpose

Source of truth for payment method and payment eligibility/status.

## Required Operations

```text
check_payment
get_plafon
get_payment_status
```

## Confirmed Business Context

The current workflow documentation explicitly includes:

```text
CHECKING_PAYMENT
```

and payment/plafon validation before confirmation. fileciteturn2file1L205-L225

The documented transaction mapping includes:

```text
payment_method
```

from Payment Rule. fileciteturn2file0L108-L126

Current analyzed examples show:

```text
PLAFON
```

but the complete production payment master is:

```text
TBD / VERIFY
```

## Required Verification

```text
payment master
plafon table/service
available limit
payment status
payment execution API
reconciliation
refund/reversal
```

---

# 16. SYS-011 — Order Draft Store / Service

## Purpose

Menyimpan order sebelum menjadi transaction.

## Required

```text
create_draft_order
get_order_draft
update_order_draft
add_order_item
remove_order_item
cancel_draft
```

## Draft Must Contain

```text
order_id
member_id
member_name
payment_method
items
subtotal
service_fee
total
status
```

## Important

Draft is mutable.

Transaction is not draft.

SOP menetapkan perubahan order dilakukan pada Order Draft dan transaction log tidak dibuat sebelum final confirmation. fileciteturn2file0L19-L44

## Required Verification

```text
where drafts are stored?
database?
Redis?
service?
Temporal payload only?
```

Preferred production design:

```text
Temporal = workflow authority
Order Service/DB = durable business order data
```

Exact implementation:

```text
TBD / VERIFY
```

---

# 17. SYS-012 — Transaction Service

## Purpose

Satu-satunya boundary yang membuat final transaction.

## Required Operation

```text
create_transaction
```

## Preconditions

```text
customer valid
product valid
quantity valid
stock valid
price valid
payment valid
customer confirmed
```

## Required Idempotency

```text
GOODANG:{order_id}
```

SOP/WI menetapkan transaction creation harus idempotent dan penggunaan idempotency key direkomendasikan dengan format tersebut. fileciteturn2file5L674-L724

## Required Verification

```text
API endpoint
transaction service
database transaction table
transaction numbering
idempotency mechanism
authorization
```

All are:

```text
TBD / VERIFY
```

---

# 18. SYS-013 — `POS_TRANSACTION_LOG`

## Status

```text
CONFIRMED
```

The current Goodang documentation identifies `POS_TRANSACTION_LOG` as the final transaction ledger.

SOP juga menetapkan bahwa setiap SKU menjadi satu baris dan transaction baru dibuat setelah customer confirmation. fileciteturn2file0L82-L126

## Confirmed Columns

| # | Column | Source |
|---:|---|---|
| 1 | `transaction_id` | System / Transaction Service |
| 2 | `transaction_date` | Tanggal order / System |
| 3 | `outlet_code` | Outlet aktif |
| 4 | `member_id` | Master Member |
| 5 | `member_name` | Master Member |
| 6 | `payment_method` | Payment Rule |
| 7 | `sku_code` | Master Product |
| 8 | `product_name` | Master Product |
| 9 | `uom` | Master Product |
| 10 | `qty` | Customer / Confirmed Order |
| 11 | `cooked_qty` | Business Rule |
| 12 | `selling_price` | Price Master |
| 13 | `service_fee` | Business Rule |
| 14 | `line_amount` | System Calculation |
| 15 | `remarks` | Customer / CS |
| 16 | `created_by` | CS / Agent identity |
| 17 | `created_at` | System Timestamp |

This 17-column structure is documented in the existing Goodang data contract. fileciteturn2file6L950-L972

## Row Rule

```text
1 transaction_id
→ many SKU lines

1 SKU
→ 1 transaction line
```

fileciteturn2file6L976-L990

---

# 19. SYS-014 — Audit Log

## Purpose

Menelusuri seluruh perubahan order/workflow.

Minimum event:

```text
WORKFLOW_STARTED
CUSTOMER_IDENTIFIED
PRODUCT_RESOLVED
PRODUCT_AMBIGUOUS
STOCK_CHECKED
PRICE_CHECKED
PAYMENT_CHECKED
DRAFT_CREATED
DRAFT_UPDATED
CUSTOMER_CONFIRMED
CUSTOMER_CANCELLED
TRANSACTION_CREATED
STOCK_DEDUCTED
ORDER_COMPLETED
HANDOVER_CREATED
```

Minimum fields:

```text
timestamp
workflow_id
order_id
member_id
actor
event
previous_state
new_state
result_code
correlation_id
metadata
```

SOP mewajibkan audit trail untuk setiap transaction. fileciteturn2file4L609-L622

Actual storage:

```text
TBD / VERIFY
```

---

# 20. SYS-015 — Human CS / Handover

## Purpose

Menangani kondisi yang tidak dapat diselesaikan agent secara aman.

Trigger minimum:

```text
customer not found
product ambiguous / unresolved
price mismatch
stock exception
payment exception
customer complaint
out of scope
system error
customer requests human
```

State:

```text
HANDOVER_CS
```

Minimum handover data:

```text
customer
telegram_chat_id
member_id
order_id
current_state
current_draft
last_customer_message
reason
error_summary
```

---

# 21. SYS-016 — Monitoring / Observability

## Minimum Metrics

```text
active workflows
completed orders
cancelled orders
handover count
transaction failures
stock failures
payment failures
tool errors
workflow failures
duplicate Telegram updates
average response latency
transaction latency
```

## Minimum Trace IDs

```text
correlation_id
telegram_update_id
telegram_chat_id
workflow_id
workflow_run_id
order_id
transaction_id
```

---

# 22. Interface Inventory

| Interface ID | Source | Target | Method | Status |
|---|---|---|---|---|
| INT-001 | Telegram | Webhook | HTTPS POST | VERIFY |
| INT-002 | Webhook | ADK | Internal function/service | TARGET |
| INT-003 | ADK | Member Service | API/tool | TBD |
| INT-004 | ADK | Product Master | API/tool | TBD |
| INT-005 | ADK | Price Service | API/tool | TBD |
| INT-006 | ADK | Stock Service | API/tool | TBD |
| INT-007 | ADK | Payment Service | API/tool | TBD |
| INT-008 | ADK | Order Draft | API/tool | TBD |
| INT-009 | ADK | Temporal | Signal/Update/command | TARGET |
| INT-010 | Temporal | Transaction Service | Activity/API | TBD |
| INT-011 | Temporal | Stock Service | Activity/API | TBD |
| INT-012 | Temporal | Payment Service | Activity/API | TBD |
| INT-013 | Transaction Service | `POS_TRANSACTION_LOG` | DB/API | TBD |
| INT-014 | Temporal | Audit | Activity/API | TBD |
| INT-015 | System | Telegram | HTTPS API | TARGET |

---

# 23. Database Inventory

Current known data stores:

```text
POS_TRANSACTION_LOG
```

Additional production data stores are not yet verified by the available source documentation.

Required discovery:

```text
Member Master
Product Master
Price Master
Stock
Payment
Order Draft
Transaction
Audit
Telegram mapping
```

For each database/table, record:

```text
system
database
schema
table
primary key
foreign keys
columns
indexes
read/write owner
retention
backup
```

Do not invent table names.

---

# 24. API Inventory

For every actual Goodang API discovered, record:

```text
API name
base URL
endpoint
HTTP method
authentication
request schema
response schema
error schema
timeout
retryable errors
idempotency support
rate limit
owner
environment
```

Template:

```yaml
api_id: API-XXX
name:
service:
base_url:
endpoint:
method:
auth:
request_schema:
response_schema:
errors:
timeout:
retry:
idempotency:
owner:
environment:
status: TBD
```

---

# 25. Authentication Inventory

Required credentials/configuration:

```text
Telegram Bot Token
Telegram Webhook Secret

ADK Model Provider Credentials

Temporal Credentials / mTLS if applicable

Member API Credentials
Product API Credentials
Price API Credentials
Stock API Credentials
Payment API Credentials
Transaction API Credentials

Database Credentials
```

Every credential must be stored outside source code.

Status of exact credentials:

```text
TBD / VERIFY
```

---

# 26. Network Inventory

Production topology should distinguish:

```text
PUBLIC
  ↓
Telegram Webhook
  ↓
API / Agent Network
  ↓
Temporal Network
  ↓
Goodang Internal Services
  ↓
Database Network
```

Do not expose to the public Internet:

```text
database
Temporal gRPC
internal Goodang APIs
```

unless there is a documented security architecture requiring it.

---

# 27. Environment Inventory

Required environments:

```text
development
staging
production
```

Recommended separation:

```text
goodang-dev
goodang-staging
goodang-prod
```

For each environment record:

```text
ADK version
model
Temporal address
Temporal namespace
task queue
Goodang API URLs
database
Telegram bot
domain
secret store
```

---

# 28. Current Known Configuration

Confirmed from current architecture documents:

```text
Primary channel:
Telegram

Agent:
Google ADK

Workflow:
Temporal

Namespace:
recommended "goodang"

Task queue:
recommended "goodang-order"

Transaction ledger:
POS_TRANSACTION_LOG
```

Exact hostnames, IPs, ports, credentials, and production endpoints:

```text
TBD / VERIFY
```

---

# 29. State Ownership

| State/Data | Owner |
|---|---|
| Conversation context | ADK Session |
| Customer identity | Member Service |
| Product identity | Product Master |
| Current price | Price Master |
| Current stock | Stock Service |
| Payment eligibility/status | Payment Service |
| Draft order | Order Service / Workflow context |
| Workflow state | Temporal |
| Final transaction | Transaction Service |
| Final transaction log | `POS_TRANSACTION_LOG` |
| Audit | Audit Service / Log Store |
| Telegram transport state | Telegram/Webhook layer |

---

# 30. Critical Data Flow

## Customer → Order

```text
Telegram
 ↓
Webhook
 ↓
ADK
 ↓
Customer Identification
 ↓
Product Resolution
 ↓
Stock Check
 ↓
Price Check
 ↓
Payment/Plafon Check
 ↓
Draft
 ↓
Confirmation
```

SOP mendefinisikan sequence tersebut. fileciteturn2file1L229-L270

---

# 31. Final Transaction Flow

```text
WAITING_CONFIRMATION
        ↓
CUSTOMER CONFIRMED
        ↓
CONFIRMED
        ↓
PROCESSING
        ↓
CREATE TRANSACTION
        ↓
TRANSACTION SUCCESS
        ↓
DEDUCT STOCK
        ↓
COMPLETED
```

SOP secara eksplisit mensyaratkan transaction final dan stock update berada setelah confirmation, dengan stock deduction setelah transaction berhasil. fileciteturn2file0L130-L148

---

# 32. Re-Order Flow

```text
CUSTOMER:
"Pesan seperti biasa."

        ↓
GET LAST ORDER
        ↓
CURRENT PRODUCT VALIDATION
        ↓
CURRENT STOCK
        ↓
CURRENT PRICE
        ↓
NEW DRAFT
        ↓
CONFIRMATION
        ↓
TRANSACTION
```

Historical order is reference, not current transaction.

---

# 33. Failure Inventory

Required handling for:

```text
Telegram unavailable
Webhook invalid secret
Duplicate Telegram update
ADK model timeout
Tool timeout
Member unavailable
Product unavailable
Price unavailable
Stock unavailable
Payment unavailable
Transaction timeout
Stock deduction timeout
Worker restart
Database restart
Duplicate confirmation
Partial transaction failure
```

For each failure:

```text
detection
retry?
max retries
state transition
customer message
handover?
reconciliation?
```

---

# 34. Critical Reliability Requirements

## Transaction

```text
idempotent
```

Key:

```text
GOODANG:{order_id}
```

## Stock

```text
idempotent
```

Key:

```text
GOODANG-STOCK:{transaction_id}
```

## Telegram

```text
deduplicate update_id
```

These rules are already required by the Temporal/Goodang implementation guidance. fileciteturn2file3L704-L724 fileciteturn2file5L772-L790

---

# 35. Security Inventory

Required controls:

```text
HTTPS
Telegram webhook secret
API authentication
database authentication
least privilege
secret management
no credential logging
no direct ADK database write
no public DB
no public Temporal gRPC
prompt injection resistance
audit
```

---

# 36. Discovery Checklist for Hermes

Hermes must complete the following before implementing production integrations:

### Server

```text
[ ] OS
[ ] CPU/RAM/Disk
[ ] Docker
[ ] Docker Compose
[ ] firewall
[ ] reverse proxy
[ ] public domain
```

### Telegram

```text
[ ] existing bot found
[ ] bot token location
[ ] webhook/polling mode
[ ] existing webhook endpoint
[ ] current bot process
```

### Goodang

```text
[ ] existing backend repository
[ ] existing APIs
[ ] authentication
[ ] database
[ ] Member Master
[ ] Product Master
[ ] Price Master
[ ] Stock
[ ] Payment
[ ] Order
[ ] Transaction
[ ] POS_TRANSACTION_LOG
```

### Temporal

```text
[ ] Temporal exists?
[ ] address
[ ] namespace
[ ] worker
[ ] task queue
[ ] persistence
[ ] UI
```

### AI

```text
[ ] ADK version
[ ] model/provider
[ ] model credentials
[ ] session persistence
```

---

# 37. Discovery Output Format

Hermes should produce:

```text
SYSTEM INVENTORY REPORT

SYSTEM:
ROLE:
ENVIRONMENT:
HOST:
PORT:
PROTOCOL:
OWNER:
SOURCE OF TRUTH:
READ ACCESS:
WRITE ACCESS:
AUTH:
STATUS:
DOCUMENTATION:
NOTES:
RISK:
```

---

# 38. Integration Readiness

Use:

```text
READY
READY_WITH_WARNING
TBD
BLOCKED
NOT_REQUIRED
```

Examples:

```text
Member API:
TBD

Product Master:
READY_WITH_WARNING

POS_TRANSACTION_LOG:
READY — schema known

Transaction API:
BLOCKED

Telegram:
READY_WITH_WARNING
```

Do not mark a system `READY` merely because a conceptual contract exists.

---

# 39. Current Known Facts vs Unknowns

## Known from source documentation

```text
Telegram is the primary target channel for this implementation.

Goodang separates:
Conversation
Order
Master Data
Transaction.

Member ID comes from Member Master.

Product identity comes from Product Master.

Selling price comes from Price Master.

Stock is checked before confirmation.

Payment/plafon is checked before confirmation.

Order draft is mutable.

Customer confirmation is required.

Transaction is created after confirmation.

Each SKU becomes one transaction-log row.

Stock deduction happens after transaction success.

POS_TRANSACTION_LOG is the final transaction ledger.
```

These facts are directly supported by the Goodang SOP/data documentation. fileciteturn2file0L106-L148 fileciteturn2file4L543-L569

## Unknown

```text
actual Member API endpoint
actual Product API endpoint
actual Price API endpoint
actual Stock API endpoint
actual Payment API endpoint
actual Order API
actual Transaction API
actual database names
actual table names
actual API authentication
actual production hosts
actual service owners
service fee formula
cooked_qty formula/semantics
stock reservation policy
refund/reversal policy
payment execution sequence
```

These must be verified before production implementation.

---

# 40. Risk Register

| Risk | Severity | Mitigation |
|---|---|---|
| Wrong customer mapping | CRITICAL | Source-of-truth member lookup |
| Wrong SKU | CRITICAL | Product Master lookup |
| Wrong price | CRITICAL | Current Price Master |
| Stock race | CRITICAL | authoritative stock service + idempotent mutation |
| Duplicate transaction | CRITICAL | transaction idempotency |
| Duplicate stock deduction | CRITICAL | stock idempotency |
| Transaction before confirmation | CRITICAL | Temporal state gate |
| Unknown payment state | CRITICAL | payment source of truth |
| Direct ADK DB mutation | CRITICAL | enforce architecture boundary |
| Missing API contract | HIGH | mark TBD; block production |
| Worker restart data loss | HIGH | Temporal durable workflow |
| Telegram duplicate update | HIGH | update_id deduplication |
| Prompt injection | HIGH | untrusted-input policy |
| Service fee ambiguity | HIGH | formalize business rule |
| cooked_qty ambiguity | MEDIUM/HIGH | formalize business rule |

---

# 41. Production Blockers

The following must block production if unresolved:

```text
[ ] No verified Member source
[ ] No verified Product source
[ ] No verified Price source
[ ] No verified Stock source
[ ] No verified Payment source
[ ] No verified Transaction service
[ ] No idempotent transaction mechanism
[ ] No idempotent stock mechanism
[ ] No customer confirmation gate
[ ] No Telegram duplicate handling
[ ] No audit trail
[ ] No rollback/reconciliation plan
[ ] Unknown service fee rule affecting transaction amount
[ ] Unknown cooked_qty semantics if field is mandatory
```

---

# 42. Recommended Discovery Sequence

```text
PHASE 1
Inspect VPS / Repository
        ↓
PHASE 2
Inspect Telegram
        ↓
PHASE 3
Inspect Goodang Backend
        ↓
PHASE 4
Inspect Database
        ↓
PHASE 5
Inspect Existing APIs
        ↓
PHASE 6
Inspect Temporal
        ↓
PHASE 7
Verify Source of Truth
        ↓
PHASE 8
Map API → Tool Contract
        ↓
PHASE 9
Map Data → Data Contract
        ↓
PHASE 10
Implement
```

---

# 43. Required Documentation After Discovery

Hermes/developer should update this inventory with:

```text
actual hostnames
actual ports
actual repositories
actual services
actual API endpoints
actual database names
actual table names
actual schemas
actual credentials mechanism
actual owners
actual environments
actual monitoring
```

Every verified item should replace `TBD`.

---

# 44. Final System Contract

```text
TELEGRAM
   ↓
WEBHOOK
   ↓
ADK
   ↓
TOOLS
   ↓
TEMPORAL
   ↓
GOODANG SOURCE-OF-TRUTH SERVICES
   ├── MEMBER
   ├── PRODUCT
   ├── PRICE
   ├── STOCK
   ├── PAYMENT
   └── TRANSACTION
            ↓
     POS_TRANSACTION_LOG
```

The Goodang SOP explicitly requires the business sequence:

```text
UNDERSTAND
    ↓
IDENTIFY
    ↓
LOOKUP
    ↓
VALIDATE
    ↓
DRAFT
    ↓
CONFIRM
    ↓
TRANSACT
    ↓
UPDATE STOCK
    ↓
COMPLETE
```

fileciteturn2file4L573-L621

---

# 45. Non-Negotiable Inventory Rules

```text
1. Do not invent an API.
2. Do not invent a database table.
3. Do not invent a column.
4. Do not invent an authentication mechanism.
5. Do not treat a documented concept as a verified production service.
6. Mark unknowns as TBD.
7. Verify every source of truth before integration.
8. Keep ADK separate from transaction mutation.
9. Keep Temporal as workflow authority.
10. Keep POS_TRANSACTION_LOG as final transaction ledger.
11. Record every production integration decision.
12. Update this inventory when infrastructure changes.
```

---

# 46. Ownership

Recommended ownership:

```text
System Architecture
→ Technical Lead

ADK Agent
→ AI/Agent Developer

Temporal
→ Backend/Workflow Developer

Telegram
→ Integration Developer

Goodang APIs
→ Goodang Backend Owner

Database
→ Database/Backend Owner

POS_TRANSACTION_LOG
→ Transaction/Operations Owner

SOP
→ Business/Product Owner

Evaluation
→ QA + AI/Agent Developer
```

Actual owner names:

```text
TBD
```

---

# 47. Inventory Maintenance

Update this document whenever:

```text
new API added
API changed
database schema changed
Telegram bot changed
Temporal namespace changed
task queue changed
new payment method added
stock logic changed
transaction logic changed
SOP changed
service ownership changed
deployment topology changed
```

---

# 48. Final Readiness Statement

At the current documentation stage:

```text
BUSINESS ARCHITECTURE
= DEFINED

ADK ARCHITECTURE
= DEFINED

TEMPORAL ARCHITECTURE
= DEFINED

DATA CONTRACT
= DEFINED

STATE MACHINE
= DEFINED

RESPONSE CONTRACT
= DEFINED

SYSTEM INVENTORY
= BASELINE DEFINED

ACTUAL PRODUCTION API / DATABASE MAPPING
= NOT YET VERIFIED
```

Therefore:

```text
NEXT STEP
=
DISCOVERY OF ACTUAL GOODANG ENVIRONMENT
```

Only after the actual systems are verified should `GOODANG_TOOL_IMPLEMENTATION_SPEC.md` be finalized.

---

# 49. Related Documents

```text
GOODANG_ADK_AGENT_SPECIFICATION.md
GOODANG_AGENT_CONTRACT.md
GOODANG_TOOL_CONTRACT.md
GOODANG_DATA_CONTRACT.md
GOODANG_TEMPORAL_WORKFLOW_SPEC.md
GOODANG_STATE_MACHINE.md
GOODANG_RESPONSE_SPEC.md
GOODANG_AGENT_EVALUATION.md
GOODANG_ADK_IMPLEMENTATION_SPEC.md
GOODANG_SYSTEM_INVENTORY.md
```

