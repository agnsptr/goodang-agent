"""ADK root agent — Fase 2: Class A tools only. See docs/9 §7."""
from __future__ import annotations

# TODO(Fase 2): from google.adk import Agent
# Register tools from app/tools/registry.yaml — Class D MUST NOT be registered.


def build_root_agent():  # type: ignore[no-untyped-def]
    """Build Goodang root agent with allowlisted tools."""
    raise NotImplementedError("Fase 2: implement with google-adk Agent + registry.yaml")
